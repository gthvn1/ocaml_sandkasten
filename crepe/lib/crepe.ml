(*
  To have a server we will need to:
    - create socket
    - bind
    - listen
    - accept (wait for client)
    - recv (wait for client)
    - send
*)

let serve () = print_endline "TODO: implement crepe server"
